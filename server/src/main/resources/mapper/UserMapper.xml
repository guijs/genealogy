<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.genealogy.mapper.UserMapper">

    <resultMap id="userResultMap" type="com.genealogy.domain.user.User">
        <constructor>
            <idArg column="id" javaType="java.util.UUID" typeHandler="com.genealogy.config.MyBatisConfig$UUIDTypeHandler"/>
            <arg column="email" javaType="java.lang.String"/>
            <arg column="password_hash" javaType="java.lang.String"/>
            <arg column="created_at" javaType="java.time.Instant"/>
        </constructor>
    </resultMap>

    <insert id="insert">
        INSERT INTO users (id, email, password_hash, created_at)
        VALUES (#{id}, #{email}, #{passwordHash}, CURRENT_TIMESTAMP)
    </insert>

    <select id="findById" resultMap="userResultMap">
        SELECT id, email, password_hash, created_at FROM users WHERE id = #{id}
    </select>

    <select id="findByEmail" resultMap="userResultMap">
        SELECT id, email, password_hash, created_at FROM users WHERE email = #{email}
    </select>

    <select id="existsByEmail" resultType="boolean">
        SELECT COUNT(*) > 0 FROM users WHERE email = #{email}
    </select>

    <delete id="deleteAll">
        DELETE FROM users
    </delete>

</mapper>
